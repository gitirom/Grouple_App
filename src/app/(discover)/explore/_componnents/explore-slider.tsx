
type Props = {
    query: string
    label: string
    text: string
}

const ExploreSlider = ({ query, label, text }: Props) => {
    return (
        <div>explore-slider</div>
    )
}

export default ExploreSlider